---
title: "版本更新"
---

# Heicode 更新日志

## 2026.05.13 · v0.1.5

### ✨ 新增功能

- 新增历史会话时间筛选功能
- 支持：
- 1 Day
- 3 Days
- 7 Days
- All
- 可通过 Tune 按钮快速切换
- 点击头像 / 邮箱可直接跳转至 Heicode Manager
- 增加 2 小时定时复查更新机制

### 🔧 功能调整

- IM 接入入口默认隐藏（当前不属于产品主范围）

### 🐞 修复与优化

- 修复 Wallet 相关问题
- 优化 StreamingIndicator 显示逻辑
- 调整审批弹窗高度
- 支持任务手动 ✓ 闭合
- 支持多选操作
- 删除社交栏相关入口
- 多项 UI 与交互细节优化

## 2026.05.14 · v0.1.9

### ✨ 新增功能

- 新增余额进度条显示
- Manager 下载页自动同步最新版本
- 更新全新应用图标

### 🐞 问题修复

- 修复跨会话流式输出异常问题
- 修复, AskUserQuestion, 渲染顺序错误
- 移除 SWA URL 依赖，彻底解决死链报错问题

### ⚙️ 工程优化

- 构建流程新增, –bundles nsis
- 接入, sccache, ，优化构建缓存与编译速度

## 2026.05.16 · v0.2.0

### 📌 版本概览 本次更新主要聚焦于 UI 一致性 与 核心稳定性提升 ，进一步完善 跨平台兼容性 。

### 🐞 问题修复

#### 修复中文字体渲染不一致问题

- 全局字体栈覆盖范围扩展至标题、正文、标签、代码等文本场景
- 新增以下 CJK 回退字体：
- PingFang SC
- Microsoft YaHei
- Noto Sans CJK SC
- 确保 macOS、Windows、Linux 下中文文字渲染风格更加统一
- 消除不同组件间字体跳变导致的视觉割裂感

#### 修复 macOS 下 ripgrep 不可用问题（cc-haha#433）

- 修复 Finder / Dock 启动 Tauri 应用时, PATH, 环境变量缺失导致无法找到, rg, 的问题
- 新增 well-known 安装路径探测机制作为兜底回退
- 当前支持探测路径：
- /opt/homebrew/bin/rg
- /usr/local/bin/rg
- ~/.cargo/bin/rg

#### 修复 Bun sidecar bundled mode 检测失败问题（cc-haha#208）

- isInBundledMode(), 原先仅检查, Bun.embeddedFiles
- 修复 Tauri sidecar 场景下误判为非 bundled 模式的问题
- 新增基于, process.execPath, 的启发式检测逻辑
- 兼容, bun build –compile, 编译产物

### 🚀 优化改进

- 统一中文字体回退策略，提升跨平台 UI 一致性
- 增强 macOS 外部命令发现能力，提高 Finder / Dock 启动稳定性
- 优化 bundled mode 检测逻辑，提升 Bun + Tauri sidecar 场景兼容性

## 2026.05.17 · v0.2.1

### 📌 版本概览 本次更新主要修复 Windows 平台显示相关问题，进一步提升整体使用体验。

### 🐞 问题修复

- 修复 Windows 系统下文件路径显示异常问题
- 现已正确显示文件名，而非完整路径
- 涉及工具调用、权限确认、文件搜索等多个场景
- 修复状态栏项目名称解析错误问题
- 修复项目过滤器在 Windows 路径下无法正确提取项目名的问题
- 修复文件搜索面包屑导航在 Windows 下不分段显示的问题
- 修复目录选择器浏览路径在 Windows 下无法正确展开层级的问题

### 🚀 优化改进

- 本次修复共影响 10 个前端组件
- 覆盖聊天界面、侧边栏、权限对话框、文件搜索、路径展示与项目识别等相关模块

## 2026.05.18 · v0.2.2

### 📌 版本概览 本次更新主要修复客户端流式任务与工具调用等待相关问题，提升长任务执行稳定性，并优化异常中断后的错误反馈体验。

### 🐞 问题修复

- 修复客户端流式任务等待时间过短问题
- 工具调用等待时间延长至, 10 分钟
- 解决此前因, 2 分钟超时, 导致任务提前中止的问题
- 修复流解析器 per-chunk 超时问题
- 两个 stream parser 均新增, 5 分钟无数据超时机制
- 上游服务长时间无响应时可抛出明确错误，而非无限等待
- 修复, fetch, 被静默中断后 UI 无错误反馈问题
- 修复后中断请求可正确结束状态并反馈错误
- 避免界面长期停留在, “Editing file…”, 状态

### 🚀 优化改进

- 提升长时间工具调用与流式响应场景稳定性
- 增强网络中断、无数据返回等异常场景错误可见性
- 优化文件编辑过程中的 UI 状态恢复逻辑

## 2026.05.19 · v0.2.4

### 📌 版本概览 本次更新主要修复自动更新模块中的并发与状态竞争问题，提升更新流程稳定性与交互一致性。

### 🐞 问题修复

#### 修复下载进度条来回跳动问题

- 原因：
- 缺少并发保护
- 多次点击会同时启动多个下载任务
- 多个进度回调交替写入 state 导致进度条回退
- 修复内容：
- 新增, isDownloading, 状态标志，防止重复触发下载
- 新增, peakPercent, 单调递增追踪机制，确保进度条永不倒退

#### 修复更新弹窗需要点击两次才显示的问题

- 原因：
- 自动检查更新与手动检查发生状态竞争
- available → checking → available, 状态反复切换导致弹窗消失
- 修复内容：
- 新增, checkInProgress, Promise 去重机制
- 重复调用, checkForUpdates, 时直接跳过
- 启动自动检查延迟由, 5 秒, 缩短为, 1 秒

### 🚀 优化改进

- 提升自动更新流程状态一致性与并发安全性
- 优化下载进度反馈逻辑，减少视觉抖动
- 改善更新弹窗触发时机与交互体验

## 2026.05.20 · v0.2.5

### 📌 版本概览 本次更新主要完成 Heicode macOS 版本 Apple 公证流程。

### 🚀 优化改进

- Heicode macOS 客户端已完成 Apple Notarization 公证
- 提升 macOS 系统安装与运行可信度
- 降低首次运行安全告警出现概率

## 2026.05.21 · v0.3.5

### 🔐 安全全面升级

- 对话数据全程加密传输，进一步加强隐私保护
- 账号绑定本机设备登录，提升账号使用安全性
- 新增多设备登录管理功能
- 网页端可查看所有登录设备
- 支持异常设备一键下线撤销

### ✨ 功能与体验优化

- 优化账户余额、资源用量展示与数据解密功能
- 模型列表支持自动实时同步
- 支持 Claude、GPT、Gemini、通义千问、DeepSeek 等主流模型
- 提升整体登录状态识别稳定性

### 🐞 问题修复

- 修复, @, 选择文件夹时无法访问目录的问题
- 支持正常访问 C 盘、D 盘等任意路径项目
- 修复模型列表展示不全与模型调用异常问题
- 修复登录后偶发显示未登录的卡顿问题
- 清理旧版本升级遗留问题
- 无需手动删除文件即可完成升级
- 修复任务执行中断、停滞不运行等异常问题

## 2026.05.22 · v0.3.6

### 📌 版本概览 本次更新主要修复客户端稳定性、权限切换异常与鉴权兼容相关问题，同时统一 Windows 与 macOS 端运行行为。

### 🐞 问题修复

#### 修复用量信息框消失问题（Windows）

- 修复升级 v0.3.5 后聊天框下方余额 / 用量信息不显示的问题
- 优化余额接口兼容逻辑，支持多路自动探测
- 即使接口不可用，也会保留信息框并提示“余额不可用”

#### 修复切换权限模式后运行报错问题

- 修复聊天过程中切换权限模式后出现：
- CLI exited during startup with code 1
- 原因：
- 旧 CLI 进程尚未完全退出
- 新 CLI 进程提前启动导致冲突
- 修复内容：
- 新进程将等待旧进程完全退出后再启动
- 超时情况下自动强制结束旧进程

#### 移除“响应等待时间过长”自动停止机制（macOS）

- 修复聊天过程中偶发：
- Error: 响应等待时间过长，本轮已自动停止
- 移除前后端回合超时 watchdog
- 与 Windows 端行为保持一致
- 优化长时间会话与流式响应稳定性

#### 移除旧版密钥兼容逻辑

- 服务端模型调用统一切换为设备签名机制
- 不再回退使用旧版, Authorization Bearer
- 提升整体安全性与鉴权一致性

### 🚀 优化改进

- 统一 Windows 与 macOS 端运行行为
- 提升 CLI 生命周期管理稳定性
- 增强长时间任务与流式响应稳定性
- 优化余额与用量信息展示兼容性
- 提升客户端整体错误恢复能力

## 2026.05.25 · v0.3.7 - v0.4.7

### 📌 版本概览 本阶段更新主要完成 V2 设备签名鉴权体系切换，并持续强化错误诊断、模型管理与客户端稳定性体验。

### 🔐 安全升级

#### 全面切换 V2 设备签名鉴权

- 服务端模型调用全面切换至 V2 设备签名鉴权
- 使用 Ed25519 私钥对每次请求进行签名
- 密钥存储于系统密钥环

#### 移除旧版 Bearer Token 兼容逻辑

- 删除旧版：
- Authorization: Bearer
- 30 天 legacy 兼容窗口正式结束

### 🚀 优化改进

- 长期 Token 不再随请求往返传输
- 实现设备级鉴权隔离
- 提升整体账户与请求安全性

### 🐞 问题修复

#### 修复余额显示错误问题

- 修复余额显示异常：
- $0.0003, 错误显示为, $155.36
- 原因：
- 服务端已返回 USD 浮点值
- 客户端重复进行 quota 单位换算

### 🚀 优化改进

- 优化余额与额度展示逻辑
- 提升账户数据准确性

### ✨ 更新弹窗与公告系统优化

#### 更新提示弹窗全面改版

- 弹窗宽度由, 24rem, 扩展至, 28rem
- Notes 区域由, 10rem, 扩展至, 22rem

#### 公告系统优化

- workflow 改为读取, RELEASE_NOTES.md
- 统一作为公告内容来源

#### UI 与排版增强

- 优化标题、按钮与 Markdown 排版效果
- 提升长更新日志可读性

### 🧠 模型管理与 CLI 稳定性修复

#### 修复模型选择器显示不可用模型问题

- 模型选择器不再显示本地, providers.json, 中不可调用的“内置”模型
- 避免触发, 401 / 404

#### 修复服务端模型兜底逻辑问题

- 服务端, /api/models, 移除, DEFAULT_MODELS, 硬编码兜底

#### 修复 provider / model 切换导致 CLI 启动失败问题

- 切换 provider / model 时自动执行, drainPendingPermissions
- 避免 transcript 残留未解决, tool_use
- 修复 CLI 启动失败问题

#### 优化模型状态显示

- 无可用模型时改为显示“模型配置”
- 不再错误显示历史模型名称

### ⚠️ 错误诊断与提示系统升级

#### 启动失败错误分类增强

- 可识别：
- device-not-paired
- 401
- 未登录
- 网络异常
- 加密失败
- 服务端拒绝等问题

#### 优化运行时 CLI 异常提示

- 运行时 CLI 退出新增友好分类提示
- 不再仅限启动阶段

#### 修复正常退出误报问题

- code 0, 正常退出不再显示红色错误 banner

#### 优化异常退出文案

- 改为中性三步排查清单
- 不再默认归因于“设备未配对”

### 🛠 错误透传与恢复能力增强

#### 新增 lastUpstreamError 全局错误寄存器

- 建立跨模块错误状态共享机制

#### V2 本地设置失败支持具体原因显示

- 支持识别：
- not_paired
- server_pubkey_failed
- encryption_failed

#### Manager 服务端拒绝请求支持详细提示

- 支持解析：
- 401 / 403
- 5xx
- 其它错误
- 自动显示可读错误消息与恢复建议

#### 修复网络异常 detail 丢失问题

- 修复, encryptedFetch, 缺少 try/catch 导致异常被吞的问题
- DNS / TLS / timeout 等异常现统一识别为：
- network_error

### 🚀 优化改进

- 提升错误透传能力
- 增强网络与鉴权问题可诊断性
- 提供更明确恢复指引

### ⚙️ 底层稳定性优化

#### 优化 stderr drain 机制

- handleProcessExit, 改为 async
- stderr drain 等待时间逐步提升：
- 500ms
- 1500ms
- 3000ms

#### 新增 Proxy 调试日志

- 新增：
- [Proxy] V2 dispatch start
- [Proxy] V2 dispatch returned
- 便于后续问题排查与诊断

### 📌 阶段总结 本阶段更新核心聚焦于两项工作：

#### 🔐 鉴权体系升级

- 将模型调用从 Bearer Token 全面切换为设备签名鉴权
- 提升安全性与设备隔离能力

#### 🛠 用户体验与诊断强化

- 将原本模糊的：
- CLI exited with code 1, 逐步细化为具体错误原因
- 每类错误均提供对应恢复建议

### 🚀 附带优化收益

- 修复余额显示错误
- 优化更新公告弹窗体验
- 模型选择器不再显示不可用模型
- code 0, 正常退出不再误报异常
- 提升长时间运行与流式响应稳定性

## 2026.05.26 – 2026.06.08 · v0.4.8 – v0.5.0

### 📌 版本概览

本阶段加强登录与账号安全、扩充可用模型，并将 macOS / Windows 客户端统一升级至 v0.5.0。

### 🔐 登录与账号安全

- 退出登录时彻底清除本机登录状态与设备绑定信息。
- 登录凭证不再保存在普通配置文件中，账号更安全。
- 修复部分情况下登录后无法正常使用（模型列表、余额无法加载）的问题。

### ✨ 模型与体验

- 新增 Gemini、GPT‑5.x Codex 等模型。
- 输入框新增「推理强度」选项。
- 网络异常时新增明确提示，错误信息更易理解。

### ⚙️ 版本

- macOS / Windows 客户端统一升级至 v0.5.0。

## **2026.06.15 · v1.0.1**

🐞 **修复与优化**

- 修复设备配对请求打错服务器导致登录后无法使用的问题（配对接口走网关地址而非登录服务地址，此前会 404 / 设备未绑定 / 模型列表与余额都拉不到）

✨ **新增功能**

- 新增邮箱 Magic-Link 登录方式
- 新增客户端错误遥测（设备级、已脱敏、默认开启，可在设置中关闭）

⚙ **工程调整**

- 客户端配置目录迁移至 `~/.heicode`（与 Claude Code 隔离，升级自动迁移）

## **2026.06.15 · v1.0.2**

✨ **新增功能**

- **任务驾驶舱（Swarm 蜂群可视化）上线**：查看 / 驱动真实蜂群运行 —— 运行列表、状态机、任务图、事件流、产物，以及资源 / 预算 / 用量 / 审计详情
- **客户端「发起蜂群」**：选择命名配置 + 填写目标即可发起蜂群任务（命名配置在 Manager 中绑定资源后生成）

🔧 **功能调整**

- 会话「Agents」控件：开启蜂群与附加云 Agent 改为**双向互斥**，避免冲突

🚀 **优化改进**

- Cloud Agent / 会话 Agents 列表刷新降频：首次成功获取后降到 1 小时，显著减少后台请求
- 任务结果按「暂无结果」与「空结果」区分展示，不再误显空结果块

## **2026.06.15 · v1.0.3**

✨ **新增功能**

- 用户菜单新增「文档中心」入口，一键打开 Heicode 文档

🔧 **功能调整**

- **任务驾驶舱并入蜂群**：在会话「Agents」里勾选「开启蜂群」并选择一个蜂群配置后，直接发送消息即发起蜂群（消息即目标）；运行进度 / 待审批移到右侧「蜂群」面板（可在面板菜单开关），左侧「驾驶舱」入口已移除

🐞 **修复与优化**

- 修复刚发起的蜂群「运行详情」偶发加载失败（事件 / 产物接口短暂不可用时优雅降级，不再整页报错）

## **2026.06.16 · v1.0.4**

🐞 **修复与优化**

- 修复**新装 / 从旧版本升级后没有登录界面、直接进入主界面**的问题（迁移用户数据但不携带旧凭据，首启走一次正式登录）
- 修复版本更新时出现**两条重叠 / 一短一长跳动**的下载进度（下载中不再被后台周期检查打断，且不再重复下载）
- 会话切换更快、不再卡在「正在加载会话…」；修复某些情况下打开后一片空白

🚀 **优化改进（蜂群）**

- 勾选「开启蜂群」从初始页 / 新建会话发起，会创建带标题的会话并自动跳转；蜂群结果与交付物作为回复显示在会话中
- 右侧「蜂群」面板可在任意页面（含 Cloud Agent）打开；蜂群事件流标签人话化（任务创建 / 执行中 / 重试 / 完成…）
- 「开启蜂群」改为按本次对话的本地开关（在 Manager 启用蜂群后才显示）

✨ **新增功能**

- 客户端最低版本支持：版本过低时提示升级

## **2026.06.17 · v1.0.5**

🐞 修复与优化

- 修复 Files 面板打开报「API error 403」的问题(浏览会话所在目录时被文件系统白名单拦截;现已正确放行当前会话目录)
- 修复重启客户端后,之前创建的蜂群会话从会话列表消失的问题(蜂群会话不再被本地 CLI 预热误删占位记录,发起时始终新建独立的容器会话)
- 修复蜂群任务完成后在同一会话继续发送指令时回显上一轮旧结果的问题 —— 完成后的后续指令改为新发起一轮,在已被上一轮更新过的工作区上继续执行

🚀 优化改进(蜂群)

- 运行面板打开即同步到最终状态,不再停留在「运行中」;事件流标签更可读
- 右侧「蜂群」面板的「待审批」只跟随当前打开的任务,不再串显其它任务的待审批
- 「Agents」控件在 Manager 已开通蜂群时正确显示「开启蜂群」选项;移除运行面板中无效的「向该 Run 发送指令」输入框

🚀 优化改进

- Files / Terminal 面板在当前会话所在目录打开(不再是默认目录)
- 模型配置加载更快:本地缓存模型列表,启动即显示,不再短暂空白

## **2026.06.17 · v1.0.6**

🐞 修复与优化

- 修复发送斜杠命令报「CLI 进程启动失败 / Provider not found」的问题(登录或配置目录迁移后旧 provider 失效时,自动回退到当前登录账号)
- 修复用 `/status`、`/cost`、`/context` 打开「会话检查器」时偶发「An unexpected error occurred」(读取失败优雅降级,不再整页报错)
- 修复打开历史会话时用量面板一律显示 0 的问题 —— 现从会话记录回填该会话真实的 token 消耗
- 修复新建会话偶发从左侧会话列表消失、且头部不显示项目目录的问题(列表刷新不再丢弃正在使用的会话)

🚀 优化改进

- Preview 面板新增「启动 dev」:检测到项目目录后,一键在后台运行 dev 脚本(npm/bun/pnpm/yarn)并自动加载预览,「停止」连带结束后台进程
- 修复 Preview 预览本地 dev 服务时页面被拦「已阻止此内容」;未启动任何服务时不再误报「本地服务启动失败」,改为友好的空闲提示
- 技能页文案品牌名修正(技能扩展 Heicode 的能力)

## 2026.06.18 · v1.0.7

🐞 修复与优化

- 修复发送图片(含读取本地图片 / 截图)后模型「看不到图 / 识别错误」的问题 —— 工具返回的图片改以正确的多模态格式送达模型;同时修复一次发送多张图片时消息顺序异常导致请求失败的问题
- 修复 PDF 解析:文本型 PDF 改用纯 JS 提取(不再依赖 poppler,修复 Windows 上「pdftoppm 不可用」导致的解析失败);图文 / 扫描型 PDF 改交模型原生读取
- 修复偶发「No such tool available」—— 容错上游把工具名改写成 `Compat<名称><随机串>` 的情况
- 收紧助手回复的分段间距;复制按钮只在整段回复末尾显示

🚀 优化改进

- 默认模型由 gpt-5.4 调整为 qwen3.7-max
- 模型协议路由优化:GPT / qwen / grok / deepseek 走原生 OpenAI 协议、Claude 走 Anthropic 协议;修复此前一律经网关转换、导致图片 / PDF 等附件内容被丢弃的问题
- 技能页文案品牌名修正(技能扩展 Heicode 的能力)

## 2026.06.18 · v1.0.8

🐞 修复与优化

图片在对话中正常显示:用 `@` 引用的图片、以及工具读取 / 截图返回的图片都会显示缩略图(不再只显示文件路径,也不再堆一长串编码);附件与图片在用户消息中右对齐

会话列表不再冒出一堆空的「Untitled Session」,并自动清理这些空占位文件;最近项目列表加载更快

新增界面崩溃兜底:渲染出错时显示可「重新载入」的提示页,不再整屏白屏

更新公告只显示当前版本的更新内容(不再堆叠历史版本)

🚀 优化改进

「需要你输入」的提问卡片优化:回答后自动折叠为一行摘要、去掉刺眼白底、整体更紧凑

减少误导性的红色感叹号:可恢复的工具结果、用户主动取消不再标红(降为琥珀 / 中性),红色只留给真正的致命错误

启动更快:按需加载图表 / diff 组件;窗口最小化时暂停后台轮询,更省电省网

执行权限模式文案调整为「请求权限」「接受编辑」

图像按尺寸压缩,减少不必要的传输(不降低识别效果)

## 2026.06.22 · v1.0.9

🐞 修复与优化

修复「自动化 / 定时任务」在安装版客户端中不执行的问题 —— 此前定时任务仅在开发环境能跑,打包后无法真正运行;现已可按计划自动执行

修复使用 GPT / qwen / grok / deepseek 等模型时,单轮并行工具调用过多(超过上游 128 上限)导致请求报错 400、会话卡住无法继续的问题 —— 客户端自动拆分为合规的多段请求

修复「自动化 → 新建任务」中模型配置下拉为空的问题 —— 现与对话框使用同一份模型列表

修复「跳过权限」确认框:弹窗位置居中(不再偏移);按 Esc 只关闭确认框,不再误关整个新建任务表单

🚀 优化改进

「自动化 → 新建任务」简化:去掉多余的「描述」必填项,只需填写任务名称与对话内容

会话管理增强:支持归档 / 取消归档、复制会话(Fork 为新会话),并新增「Active / Archived / All」状态筛选

标题栏与「设置 → 关于」新增「预览」标识
